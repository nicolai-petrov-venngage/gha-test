
name: nicolai-test

on:
  push:
    branches:
      - nicolai-test-*

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: .
    steps:
      # - name: Check out code
      #   uses: actions/checkout@v6

      - name: Echo current commit SHA
        run: 'echo "Current commit SHA: $GITHUB_SHA"'


